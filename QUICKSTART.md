# 🚀 Quick Start Guide

Быстрое руководство по запуску Testing Platform за 5 минут.

## Предварительные требования

- Docker и Docker Compose установлены
- Git установлен

## Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd EVROSHOLS
```

## Шаг 2: Настройка окружения

Создайте файл `.env` на основе примера:

```bash
cp env.example .env
```

Откройте `.env` и измените критически важные параметры:

```env
# ОБЯЗАТЕЛЬНО измените этот ключ на случайную строку!
SECRET_KEY=your-random-secret-key-minimum-32-characters-long

# Установите надежный пароль для базы данных
POSTGRES_PASSWORD=your_secure_password_here
```

💡 **Совет**: Для генерации SECRET_KEY используйте:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

## Шаг 3: Запуск приложения

```bash
# Запустите все сервисы
docker-compose up -d

# Проверьте что все запустилось
docker-compose ps
```

Вы должны увидеть 4 запущенных контейнера:
- testing_platform_postgres
- testing_platform_redis
- testing_platform_backend
- testing_platform_frontend

## Шаг 4: Применение миграций

```bash
# Создайте структуру базы данных
docker-compose exec backend alembic upgrade head
```

## Шаг 5: Создание первого администратора

```bash
# Запустите скрипт создания админа
docker-compose exec backend python create_admin.py
```

Это создаст пользователя:
- **Username**: `admin`
- **Password**: `admin123`
- **Role**: Admin

⚠️ **ВАЖНО**: Смените пароль после первого входа!

## Шаг 6: Открытие приложения

Откройте в браузере:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API Documentation**: http://localhost:8000/docs

## Шаг 7: Первый вход

1. Откройте http://localhost:5173
2. Нажмите "Sign in"
3. Введите:
   - Username: `admin`
   - Password: `admin123`
4. Нажмите "Sign in"

Поздравляем! 🎉 Вы запустили Testing Platform!

## Что дальше?

### Создайте тестовых пользователей

В интерфейсе администратора перейдите в "Manage Users" и создайте:

1. **Преподавателя**:
   - Username: `teacher1`
   - Role: Teacher
   - Email: teacher@example.com

2. **Студента**:
   - Username: `student1`
   - Role: Student
   - Email: student@example.com

3. **Родителя**:
   - Username: `parent1`
   - Role: Parent
   - Email: parent@example.com

### Создайте первый тест

1. Войдите как преподаватель
2. Перейдите в "My Tests"
3. Нажмите "Create Test"
4. Заполните информацию о тесте
5. Добавьте вопросы
6. Сохраните тест
7. Назначьте тест студенту

### Пройдите тест

1. Войдите как студент
2. Перейдите в "My Tests"
3. Выберите назначенный тест
4. Нажмите "Start Test"
5. Ответьте на вопросы
6. Нажмите "Submit Test"
7. Просмотрите результаты

## Полезные команды

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Остановка приложения

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить все данные
docker-compose down -v
```

### Перезапуск приложения

```bash
docker-compose restart
```

### Обновление приложения

```bash
# Остановите сервисы
docker-compose down

# Получите последние изменения
git pull

# Пересоберите образы
docker-compose build

# Запустите снова
docker-compose up -d

# Примените миграции
docker-compose exec backend alembic upgrade head
```

## Решение проблем

### Backend не запускается

Проверьте логи:
```bash
docker-compose logs backend
```

Частые проблемы:
- База данных еще не готова → подождите 10-20 секунд
- Неправильный DATABASE_URL в .env
- Порт 8000 уже занят → измените в docker-compose.yml

### Frontend не запускается

Проверьте логи:
```bash
docker-compose logs frontend
```

Частые проблемы:
- Backend не запустился
- Порт 5173 занят → измените в docker-compose.yml
- VITE_API_URL неправильно настроен

### База данных не подключается

```bash
# Проверьте статус PostgreSQL
docker-compose exec postgres pg_isready

# Подключитесь к базе вручную
docker-compose exec postgres psql -U postgres -d testing_platform
```

### Не могу войти

1. Убедитесь, что создан админ:
```bash
docker-compose exec backend python create_admin.py
```

2. Проверьте правильность учетных данных

3. Проверьте логи backend на ошибки

## Использование Makefile (опционально)

Если у вас установлен `make`:

```bash
# Показать все команды
make help

# Запустить приложение
make up

# Остановить приложение
make down

# Просмотр логов
make logs

# Применить миграции
make migrate

# Создать админа
make create-admin
```

## Дополнительные ресурсы

- [README.md](README.md) - Полная документация
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- [API Documentation](http://localhost:8000/docs) - Swagger UI (после запуска)

## Поддержка

Если у вас возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь что все контейнеры запущены: `docker-compose ps`
3. Проверьте .env файл
4. Перезапустите приложение: `docker-compose restart`

---

**Приятного использования Testing Platform!** 🎓

