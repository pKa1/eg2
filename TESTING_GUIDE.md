# 🧪 Руководство по автоматическому тестированию

## Обзор

В проекте созданы автоматические скрипты для создания тестов со всеми типами вопросов и их назначения студентам.

## Доступные команды

### 1. Создать тест со всеми типами вопросов

```bash
make test-all-types
```

**Что делает:**
- Создает новый тест с названием "🧪 Полный тест всех типов вопросов"
- Добавляет по одному вопросу каждого из 11 типов
- Устанавливает статус "published"
- Общий балл: 20.5 баллов

### 2. Назначить тест конкретным студентам

```bash
make assign-test TEST_ID=3
```

**Что делает:**
- Назначает указанный тест всем студентам в системе
- Проверяет дубликаты (не назначит повторно)
- Выводит статистику назначений

### 3. Создать и назначить тест одной командой ⭐

```bash
make setup-full-test
```

**Что делает:**
- Создает тест со всеми типами вопросов
- Автоматически назначает его всем студентам
- Выводит ID теста для дальнейшего использования

## Типы вопросов в тесте

| # | Тип | Название | Баллы | Описание |
|---|-----|----------|-------|----------|
| 1 | `single_choice` | Одиночный выбор | 1.0 | Вопрос о планетах |
| 2 | `multiple_choice` | Множественный выбор | 2.0 | Языки программирования |
| 3 | `true_false` | Правда/Ложь | 1.0 | JavaScript vs Java |
| 4 | `short_answer` | Короткий ответ | 1.5 | Термин "компиляция" |
| 5 | `essay` | Эссе | 3.0 | Stack vs Heap |
| 6 | `matching` | Сопоставление | 2.0 | Языки и применение |
| 7 | `fill_in_blank` | Заполнить пропуски | 2.0 | Столицы стран |
| 8 | `ordering` | Упорядочивание | 2.0 | Waterfall модель |
| 9 | `numeric` | Числовой ответ | 1.0 | Байты в килобайте |
| 10 | `file_upload` | Загрузка файла | 2.0 | Скриншот IDE |
| 11 | `code` | Написание кода | 3.0 | Функция факториал |

## Сценарии использования

### Сценарий 1: Первый запуск платформы

```bash
# 1. Создать админа
make create-admin

# 2. Создать и назначить полный тест
make setup-full-test

# 3. Создать демо-студента (если нужен)
make create-demo-student
```

### Сценарий 2: Добавление нового типа теста

```bash
# Создать только тест (без назначения)
make test-all-types

# Затем назначить вручную нужным студентам
make assign-test TEST_ID=<полученный_id>
```

### Сценарий 3: Проверка всех типов вопросов

1. Запустите:
   ```bash
   make setup-full-test
   ```

2. Войдите как студент:
   - Email: `student@demo.com`
   - Пароль: `demo123`

3. Пройдите тест и проверьте:
   - ✅ Все типы вопросов отображаются корректно
   - ✅ Можно ответить на каждый вопрос
   - ✅ Отправка теста работает без ошибок

4. Войдите как преподаватель/админ:
   - Email: `admin@example.com`
   - Пароль: `admin123`

5. Проверьте результаты студента

## Структура скриптов

### `create_test_all_types.py`
Создает комплексный тест со всеми 11 типами вопросов. Каждый вопрос содержит:
- Текст вопроса
- Правильный ответ (где применимо)
- Объяснение
- Варианты ответов (для типов с опциями)

### `assign_test_to_students.py`
Назначает указанный тест всем студентам:
- Проверяет существование теста
- Находит всех студентов
- Создает записи в `test_assignments`
- Избегает дубликатов

### `setup_full_test.py`
Мастер-скрипт, объединяющий оба предыдущих для быстрого старта.

## Проверка результатов

### Проверить созданные тесты:

```bash
# Через psql
make shell-db
SELECT id, title, status, creator_id FROM tests;

# Проверить вопросы
SELECT id, question_type, points FROM questions WHERE test_id = 3;
```

### Проверить назначения:

```sql
SELECT 
  ta.id,
  t.title AS test_name,
  u.full_name AS student_name,
  ta.created_at
FROM test_assignments ta
JOIN tests t ON ta.test_id = t.id
JOIN users u ON ta.student_id = u.id;
```

## Решение проблем

### Ошибка: "Тест не создан"
- Проверьте, что backend запущен: `docker-compose ps`
- Проверьте логи: `make logs-backend`

### Ошибка: "Студенты не найдены"
- Создайте студентов:
  ```bash
  make create-demo-student
  ```

### Ошибка при назначении
- Убедитесь, что указан правильный TEST_ID
- Проверьте, что тест существует в БД

## Расширение

### Добавить свой тип теста

1. Создайте новый файл, например `create_custom_test.py`
2. Используйте `create_test_all_types.py` как шаблон
3. Добавьте команду в `Makefile`:
   ```makefile
   custom-test: ## Create custom test
       docker-compose exec backend python create_custom_test.py
   ```

### Создать тест для конкретной темы

Измените скрипт `create_test_all_types.py`, заменив вопросы на вопросы по вашей теме, но сохраняя все 11 типов для полной проверки функциональности.

## Чеклист тестирования

После создания теста проверьте:

- [ ] Тест создан и виден в списке тестов
- [ ] Все 11 вопросов присутствуют
- [ ] Студенты видят назначенный тест
- [ ] Можно начать прохождение теста
- [ ] Все типы вопросов отображаются корректно
- [ ] Можно ответить на каждый вопрос
- [ ] Можно отправить тест
- [ ] Результаты сохраняются
- [ ] Преподаватель видит результаты
- [ ] Баллы подсчитываются правильно

## Полезные команды

```bash
# Посмотреть все доступные команды
make help

# Проверить статус сервисов
docker-compose ps

# Логи backend
make logs-backend

# Логи frontend  
make logs-frontend

# Войти в backend shell
make shell-backend

# Войти в PostgreSQL
make shell-db
```

---

**Последнее обновление:** 5 ноября 2025

